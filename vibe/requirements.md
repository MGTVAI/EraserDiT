# 加速框架迁移需求

## 目标

以 MGErase 架构为基础重构当前仓库，接入 EraserDiT 的擦除算法及 MGErase 加速能力，在当前环境完成端到端推理，获得可重复的实际加速。框架应支持后续接入其他模型。

- 来源：`/mnt/shanhai-ai/shanhai-workspace/zhouhao6/MGErase`。
- 对齐基准：当前 EraserDiT 原版输出。
- 使用场景：支持 CLI 运行，也支持 server 端运行。
- 不兼容旧入口、参数、接口和目录；原版仅通过冻结版本作为独立基线。

## 范围与顺序

1. 建立新架构、接入原版算法行为，支持 CLI 运行和基础推理服务。
2. 完成单卡加速：注意力后端、算子融合、Transformer 编译。
3. 依次扩展缓存、量化、多卡并行。

第一阶段不通过减少采样步数或降低分辨率获取加速。硬件不支持的路径不得宣称通过当前环境验收。

## 目录与入口

- 目录以 `MGErase/python/` 为主，内容迁入当前仓库根目录，不保留 `python/` 容器。
- 不保留 `runtime/` 容器，其直接子目录提升到根目录；其中 `runtime/resource/` 与 `layers/memory/` 合并为根目录 `memory/`。
- `memory/` 下按 `adapters/`、`backends/`、`policies/` 分别组织模型适配、搬运卸载实现和驻留及阶段协调策略，不再保留 `resource/` 或 `layers/memory/`。第一阶段只要求三者的职责边界与策略接口就位，具体驻留策略在环境核查时按实测可用显存确定并记录。
- 原版 `models/`、`pipelines/`、`utils/` 下的内容按职责迁入新目录，作为 EraserDiT 首个接入模型；旧入口 `inference.py` 及其参数面随迁移删除，迁移验证通过后删除其余被替代的实现。
- 注意力、融合、量化等计算模块沿用 `layers/` 下的组织方式。
- 根目录保留 `vibe/` 存放需求及开发方案，`docs/` 存放使用与技术文档，`docker/` 存放容器构建及环境配置。
- 根目录提供 `inference_cli.sh` 和 `inference_server.sh`，共享配置、加载器、管线及加速实现。
- 第一阶段单 GPU、任务顺序执行。

## 基线冻结

- 当前工作区修改提交后打基线 tag，并另存一份独立副本供验收反复运行；主仓库完成重构后即可删除被替代的原版实现，基线仅以该副本存在。
- 冻结内容包含提交标识、工作区差异、权重快照标识、全部采样参数与提示词、环境版本，以及两组素材的基准视频。
- 冻结副本允许两类最小外挂改动，均不触碰算法参数：注入并记录全部随机源以固定 seed；增加常驻 wrapper，在同进程内重复调用原版推理入口。
- 冻结副本相对原版的全部差异单独记录，验收时说明这些改动不影响生成结果。

## 服务端

以 MGErase `entrypoints/` 与 `runtime/service/` 的实现为参考，保留其协议骨架、任务生命周期和服务组件划分：

- 端点：`POST /v1/videos`、`GET /v1/videos`、`GET /v1/videos/{id}`、`DELETE /v1/videos/{id}`、`GET /v1/videos/{id}/content`、`GET /v1/models`、`GET /v1/models/{id}`、`GET /health`、`GET /server_info`、`GET /model_info`、`GET /stats`。
- 任务状态为 `queued`、`running`、`completed`、`failed`、`cancelled`；阶段为 `queued`、`preparing`、`processing`、`finalizing`、`terminal`；响应携带进度、队列位置、分段进度及指标。
- 服务组件按任务存储、任务产物管理、调度器和常驻工作组划分；常驻工作组持有模型实例并对外提供健康心跳。
- 请求契约拒绝未声明字段，统一错误体为 `{"error": {"code", "message"}}`。
- 模型相关部分由模型适配器提供参数 schema、采样参数 builder 和 capability 标识；服务骨架不写死模型实现，接入第二个模型时不改写服务代码。
- 第一阶段只支持本地路径提交（受输入白名单约束）和本地结果存储；文件上传、对象存储、多卡及量化参数不进入参数面。
- 参数默认值与字段集按 EraserDiT 原版算法定义，不沿用 MGErase 的模型专属字段。
- `server_info` 的启动配置必须包含实际生效的加速后端、融合与编译开关，以及 `auto` 回退后的实际选择及原因。

## CLI

- 接受单个任务参数，或一个 JSON 任务文件；任务文件为任务数组，每项含视频、mask、提示词及参数覆盖。
- 顺序执行，结果按任务 id 落盘；任务结构与服务端一致，两者共用执行链路。

## 验收

验收在 `EraserDiT` conda 环境中进行，硬件为当前机器的 A100；`docker/` 下的镜像作为可复现交付物单独验证构建与启动，不作为加速数字的基准。

使用仓库自带的两组素材：

- `data/10268234.mp4`、`data/10268234_mask.mp4`。
- `data/113000356.mp4`、`data/113000356_mask.mp4`。

原版、新架构未加速版及加速版使用相同权重、输入、提示词、种子、采样参数、尺寸和分段设置。两组素材的提示词在基线冻结时固定并记录。

视频要求：

- 完整输出可解码，分辨率、帧数、帧率符合基线，无丢帧、黑帧或异常接缝。
- 允许颜色、局部纹理和生成细节差异；明显擦除失败、目标残留、画面损坏或新增明显时序退化不通过。
- 查看完整视频、擦除区域、mask 边缘和非擦除区域；逐帧指标辅助判断，不以单一分数替代视频验收。

性能要求：

- 主要指标是模型加载与预热完成后，从读取输入到最终视频写出完成的端到端耗时。
- 基线与候选配置均以常驻方式运行，同 GPU、可比负载下重复测量；两组素材分别报告耗时中位数、加速倍数、去噪耗时和峰值显存。
- 正式测量在协调出的独占（或同卡无其他大任务）窗口内进行；共享卡上取得的对比结果需标注当时的占用状态。
- 预热覆盖两组素材的尺寸及其正段、尾段形状；首次启动、编译及未覆盖形状的额外开销计入该次任务并单独列出，不隐藏在统计之外。
- 不预设加速倍数；推荐配置必须取得可重复的实际收益。

## 交付物

- 新架构代码、两个启动脚本、环境及使用说明、服务端接口文档。
- 冻结的原版基线副本、可复现的加速配置，以及连续任务验收脚本与任务文件样例。
- 两组素材的原版、加速版、并排对比视频，以及性能和差异报告。
- 当前环境实测有效的推荐加速配置及组合限制，第一阶段为注意力、融合、编译三项的组合。
